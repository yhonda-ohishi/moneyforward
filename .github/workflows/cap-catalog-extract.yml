name: cap-catalog extract

# Nuxt 4 SPA は `app/` 配下なので working_directory を渡す。
# default branch は `master`。
# Refs ippoan/cap-catalog#29, ippoan/cap-catalog#31

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

# Per-job permission パターン (Refs ippoan/cap-catalog#29 retroactive 改善):
# top-level は read のみ。auto-merge job 側で write を明示 elevate する。
permissions:
  contents: read

jobs:
  extract:
    uses: ippoan/ci-workflows/.github/workflows/catalog-extract.yml@main
    with:
      language: ts
      working_directory: app

  # CI 完走後に PR を auto-merge する (Refs ippoan/cap-catalog#29 Phase 1 pattern)。
  # cross-org caller (yhonda-ohishi → ippoan/ci-workflows) なので `secrets: inherit`
  # が効かない。CI_APP_ID / CI_APP_PRIVATE_KEY は yhonda-ohishi の repo secret 前提。
  auto-merge:
    needs: [extract]
    if: github.event_name == 'pull_request'
    permissions:
      contents: write
      pull-requests: write
    uses: ippoan/ci-workflows/.github/workflows/auto-merge.yml@main
    secrets:
      CI_APP_ID: ${{ secrets.CI_APP_ID }}
      CI_APP_PRIVATE_KEY: ${{ secrets.CI_APP_PRIVATE_KEY }}
